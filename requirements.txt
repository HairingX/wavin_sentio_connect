#install the required packages by running the following command: 
#pip install --require-virtualenv -r requirements.txt
#This will install the required packages in the current environment
#You can update the packages if they have been updated to newer versions by running the command:
#pip install --require-virtualenv -r requirements.txt --upgrade
setuptools
pytest
pytest-asyncio
# modbus_event_connect ~=0.1
modbus_event_connect @ git+https://github.com/HairingX/modbus_event_connect